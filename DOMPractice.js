let findele;
{
    // findele = document.querySelectorAll("div");
    // findele[0].innerText = findele[0].innerText + "\nCredit : Madhav";
    // console.log(findele[0].innerText);

    // findele = document.querySelectorAll("div");
    // findele[1].innerText = findele[1].innerText + "\nCredit : Madhav";
    // console.log(findele[1].innerText);


    // findele = document.body.children;
    // console.log(findele);
}


let findatt;
{
    // DOM manipulation

    // Attributes 
    // getAttribute(attr) // to get attribute value
    // setAttribute(attr,value) // to set the attribute val th

    // findatt = document.querySelectorAll("div");
    // console.log(findatt);
    // let cls = findatt[0].getAttribute("class");
    // console.log(cls);

    // let changeatt = findatt[0].setAttribute("class","streaming");
    // console.log(changeatt);

    // access style attribute [node.style]

    // findatt = document.getElementsByTagName("div")
    // console.log(findatt[0].style);
    // findatt[0].style.backgroundColor = "pink";

    // insert element
    // first we have to create ele : 
    // let  ele = document.createElement("button");
    // ele.innerText = "Click me!";
    // console.log(ele.innerText);
    // node.append(ele); // adds at the end of node(inside)

    // findatt = document.getElementsByTagName("div");
    // findatt[0].append(ele);


    // node.prepend(ele); // adds at the start of node(inside)

    // findatt[0].prepend(ele);

    // node.before(ele); // adds before the node(outside)
    // findatt[0].before(ele);

    // node.after(ele); // adds after the node(outside)
    // findatt[0].after(ele);

    // create a h1 element
    // let heading = document.createElement("h1");
    // heading.innerHTML = "<b><i>Netflix Shows</i></b>";

    // prepend it in body tag
    // document.querySelector("body").prepend(heading);

    // delete node : 
    // node.remove()

    // ele.remove();

}